@model IEnumerable<StoresManagement.Models.Entity>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>


@*
    Note: I've set the model for this view to IEnumerable<Entity>.
    This is a simple interface implemented by the list class. Since
    in this view we only want to iterate over this list, and we don't
    need any of the operations in the List class (eg Add, Remove, etc),
    it's better to use the IEnumerable interface, which allows use to
    iterate over the list. If in the future, we replace the List with a
    different data structure, as long as it is enumerable, our view code
    will remain unchanged.
*@


<p>
    @Html.ActionLink("New Entity", "Create", "Entities", null, new { @class = "btn btn-primary" })
</p>

<table id="entities" class="table table-bordered table-hover">
    <thead>
        <tr>
            <th>Entity</th>
            @*<th>Membership Type</th>*@
            <th>Delete</th>
        </tr>
    </thead>
    <tbody></tbody>
</table>

@section scripts
{
    <script>
        $(document).ready(function () {
            var table = $("#entities").DataTable({
                ajax: {
                    url: "/api/entities",
                    dataSrc: ""
                },
                columns: [
                    {
                        data: "name",
                        render: function (data, type, entity) {
                            return "<a href='/entities/edit/" + entity.id + "'>" + entity.name + "</a>";
                        }
                    },
                    //{
                    //    data: "membershipType.name"
                    //},
                    {
                        data: "id",
                        render: function (data) {
                            return "<button class='btn-link js-delete' data-entity-id=" + data + ">Delete</button>";
                        }
                    }
                ]
            });

            $("#entities").on("click", ".js-delete", function () {
                var button = $(this);

                bootbox.confirm("Are you sure you want to delete this entity?", function (result) {
                    if (result) {
                        $.ajax({
                            url: "/api/entities/" + button.attr("data-entity-id"),
                            method: "DELETE",
                            success: function () {
                                table.row(button.parents("tr")).remove().draw();
                            }
                        });
                    }
                });
            });
        });
    </script>
}